export default class Form{
    constructor(title, date, status, projectSignature){
        this.title = title;
        this.date = date;
        this.status = status;
        this.projectID = projectSignature;
    }
}